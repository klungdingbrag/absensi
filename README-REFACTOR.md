# v1.0.1 Refactor

Frontend is separated into HTML, CSS, and JavaScript modules. This branch is for testing before merging into `main`.

Modules: config, state, utils, employee, attendance, payroll, cloud, render, print, export, app.