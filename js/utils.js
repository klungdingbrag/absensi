function setStatusSync(type,text){const el=document.getElementById("syncText");if(el)el.innerText=text;const dot=document.getElementById("syncDot");if(dot)dot.className="dot "+type;if(type==="active"){let last=document.getElementById("syncLast");if(!last&&el){last=document.createElement("span");last.id="syncLast";el.parentElement?.appendChild(last)}if(last)last.innerText=`Terakhir sinkron ${new Date().toLocaleTimeString("id-ID",{hour:"2-digit",minute:"2-digit"})}`;showAppToast("success",text)}}
function showAppToast(type,text){let box=document.getElementById("appToastContainer");if(!box){box=document.createElement("div");box.id="appToastContainer";box.className="app-toast-container";document.body.appendChild(box)}const toast=document.createElement("div");toast.className=`app-toast ${type||"info"}`;toast.innerHTML=`<span class="app-toast-icon">${type==="success"?"✓":type==="error"?"!":"i"}</span><span>${String(text).replace(/[&<>\"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[m]))}</span>`;box.appendChild(toast);requestAnimationFrame(()=>toast.classList.add("show"));setTimeout(()=>{toast.classList.remove("show");setTimeout(()=>toast.remove(),250)},3200)}
function formatRupiah(n){return new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(Number(n)||0)}
function formatDateISO(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
function formatTanggalIndonesia(s){return new Date(s+"T00:00:00").toLocaleDateString("id-ID",{day:"numeric",month:"long",year:"numeric"})}
function verifikasiPin(){const x=prompt("Masukkan PIN Admin:");if(x===CONFIG.PIN_ADMIN)return true;alert("PIN Salah!");return false}
