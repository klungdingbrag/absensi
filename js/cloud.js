function getPeriodeMinggu(){const input=document.getElementById("tglMulai").value;if(!input)return null;const start=new Date(input+"T00:00:00");start.setDate(start.getDate()-start.getDay());const end=new Date(start);end.setDate(end.getDate()+6);return {start:formatDateISO(start),end:formatDateISO(end)}}
function updatePeriodeTanggal(autoLoad=false){const p=getPeriodeMinggu();if(!p)return;clearTimeout(timerSave);timerSave=null;document.getElementById("tglMulai").value=p.start;document.getElementById("tglSelesai").value=p.end;const start=new Date(p.start+"T00:00:00");HARI.forEach((h,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);document.getElementById("date-"+h).innerText=d.toLocaleDateString("id-ID",{day:"numeric",month:"short"})});document.getElementById("periodeAktif").innerText=`PERIODE AKTIF: ${formatTanggalIndonesia(p.start)} — ${formatTanggalIndonesia(p.end)}`;if(autoLoad)muatDataDariCloud()}
function pindahMinggu(delta){const p=getPeriodeMinggu();if(!p)return;clearTimeout(timerSave);timerSave=null;const d=new Date(p.start+"T00:00:00");d.setDate(d.getDate()+delta*7);document.getElementById("tglMulai").value=formatDateISO(d);updatePeriodeTanggal(true)}
async function muatDataDariCloud(){if(loadingWeek)return;loadingWeek=true;const p=getPeriodeMinggu();if(!p){loadingWeek=false;return}setStatusSync("loading",`Mengambil data ${p.start} s/d ${p.end}...`);try{const res=await fetch(`${CONFIG.SCRIPT_URL}?action=get&weekStart=${encodeURIComponent(p.start)}`,{cache:"no-store"});const result=await res.json();if(!result.success)throw new Error(result.message||"Gagal membaca cloud");if(Array.isArray(result.data)&&result.data.length){dataKaryawan=result.data.map(normalisasiKaryawan)}else{dataKaryawan=DEFAULT_KARYAWAN.map(k=>buatStrukturKaryawan(k.nama,k.gajiPokok));await simpanDataKeCloudDirect(true,p)}setStatusSync("active",`Cloud aktif — ${formatTanggalIndonesia(p.start)} s/d ${formatTanggalIndonesia(p.end)}`);renderTabel()}catch(e){console.error(e);setStatusSync("error","Gagal memuat data. Periksa URL Apps Script & koneksi.")}finally{loadingWeek=false}}
function pemicuAutoSave(){renderTabel();clearTimeout(timerSave);const p=getPeriodeMinggu();if(!p)return;const snapshot=JSON.parse(JSON.stringify(dataKaryawan));setStatusSync("loading","Perubahan menunggu disimpan...");timerSave=setTimeout(()=>simpanDataKeCloudDirect(false,p,snapshot),800)}
async function simpanDataKeCloudDirect(silent=false,periodeOverride=null,dataOverride=null){const p=periodeOverride||getPeriodeMinggu();if(!p)return;const payloadData=dataOverride||dataKaryawan;try{if(!silent)setStatusSync("loading",`Menyimpan ${formatTanggalIndonesia(p.start)} s/d ${formatTanggalIndonesia(p.end)}...`);const payload={action:"save",weekStart:p.start,weekEnd:p.end,data:payloadData};await fetch(CONFIG.SCRIPT_URL,{method:"POST",mode:"no-cors",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify(payload)});if(!silent)setStatusSync("active",`Tersimpan — ${formatTanggalIndonesia(p.start)} s/d ${formatTanggalIndonesia(p.end)}`)}catch(e){console.error(e);setStatusSync("error","Gagal menyimpan perubahan!")}}
